<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>All Posts</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <div class="container">
    <h1>All Posts</h1>

    <% for(post of posts) { %>
      <div class="post">
        <h3 class="user">@<%= post.username %></h3>
        <p class="content"><%= post.content %></p>

        <a class="btn" href="/posts/<%= post.id %>">See in detail</a>
        <a class="btn" href="/posts/<%= post.id %>/edit">Edit</a>
        <form method="post" action="/posts/<%= post.id %>?_method=DELETE" style="display:inline;">
          <button class="btn btn-danger">Delete</button>
        </form>
      </div>
    <% } %>

    <div class="nav-btn">
      <a class="btn" href="/posts/new">Create New Post</a>
    </div>
  </div>
</body>
</html>
